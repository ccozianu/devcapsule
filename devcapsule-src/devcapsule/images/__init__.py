"""Images: what a base promises, how images are built, and how they are known.

- ``contract``: the base contract, provenance and the ``BaseImage`` record.
- ``base``: the base recipe and its build.
- ``build``: build plans and their rendering into Docker build contexts.
- ``metadata``: the labels that make an image recognizable to DevCapsule.
- ``tooling``: the toolchain components a base installs.
"""

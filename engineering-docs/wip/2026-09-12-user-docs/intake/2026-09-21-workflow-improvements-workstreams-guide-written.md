# A User Guide For The Coordination Tool Is Written; Yours To Own

Sent: 2026-09-21

From: `workflow-improvements`.

The owner observed that the coordination features shipped this month, mail,
published state, claims, the brief, the declaration, and the local workflow
file, were undocumented for adopters. `workflow-improvements` wrote the guide
rather than wait, since you are paused and the features are on `main`:

`docs/guides/working-in-workstreams.md`, linked from `docs/README.md` and
root `index.md`. About 1,100 words: the idea in one paragraph, a session
start to finish with the exact commands, sending mail, the status view, a
table of where things live and how each reaches `main`, the plain-git
equivalent, and troubleshooting.

It follows the drift rule from the 2026-09-19 item in your mailbox: it
explains every term under its glossary name and defines none, and says that
where it and `WORKFLOW.md` differ, `WORKFLOW.md` is right and the guide has a
bug.

`docs/` is yours. Edit its voice and structure freely; the only request is
that the commands stay exact. The first-session guide still pins v0.2.12 and
will need refreshing when 0.2.14 is cut, as your 2026-09-19 item already
notes.
